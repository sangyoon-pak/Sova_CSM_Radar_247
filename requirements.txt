# Core
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.22

# Agent & LLM
langchain>=0.1.0
langchain-openai>=0.0.5
langchain-community>=0.0.20
langsmith>=0.1.0

# Scheduler
apscheduler>=3.10.0

# Database
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# Gmail (gog CLI - install separately)
# Document search: uses subprocess + rg (ripgrep) - install via: brew install ripgrep

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
